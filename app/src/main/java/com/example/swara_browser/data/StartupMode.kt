package com.example.swara_browser.data

enum class StartupMode(val displayName: String) {
    OPEN_NEW_TAB("Open the New Tab page"),
    CONTINUE_LAST("Continue where you left off"),
    SPECIFIC_PAGE("Open a specific page")
}
